# 핸드오프 보드

규약은 [README.md](README.md) 참고. 최신 요청이 위로 온다.

## 레퍼런스

사람이 ChatGPT 에 함께 올린 디자인 레퍼런스. `inbox/` 에 보관.

| 파일 | 내용 |
|---|---|
| `inbox/ref_map_screen.png` | 지도 화면 목업 (852x1846). 전신 렉시 말풍선, 아이소메트릭 층 지도, 주황 마름모 POI 마커, 하단 목적지 버튼, 탭바 |
| `inbox/ref_map_screen_with_qr_warning.png` | 위와 동일 + "주변이 어둡습니다 / QR 다시 찍기" 경고 배너, 1F 층 선택, 나침반 |

### 결과물 성격 구분 (중요)

GPT 가 주는 이미지는 두 종류이고 쓰임이 다르다.

- **바로 쓰는 것** — 앱 아이콘, 개별 스프라이트(마커 아이콘, 버튼 배경, 탭바
  아이콘). 투명 배경 PNG 로 낱장이면 그대로 프로젝트에 넣는다.
- **참고만 되는 것** — 화면 전체 목업. 이건 앱에 넣을 수 없다. UMG 위젯으로
  다시 만들어야 한다. 목업 한 장을 통째로 화면에 깔면 버튼도 안 눌리고
  해상도도 안 맞는다.

목업만 받으면 구현 근거로만 쓰이고, 실제 작업량은 줄지 않는다. 화면에 넣을
그림이 필요하면 **낱장 스프라이트**로 따로 요청해야 한다.

---

## REQ-002 — 화면 스타일 개선용 에셋

**상태: OPEN**
요청자: Claude / 등록: 2026-09-10

현황 분석은 [UI_GAP.md](UI_GAP.md) 참고. 두 화면 모두 이미 동작하므로
필요한 것은 스타일과 에셋 몇 개뿐이다.

### 이미 있어서 요청하지 않는 것

에셋을 새로 만들기 전에 저장소를 뒤졌다. 목업 요소 대부분이 이미 있다.

| 목업 요소 | 기존 파일 |
|---|---|
| 말풍선 배경 | `Asset/Docent/talkbox.png` |
| 전신 렉시 (가리키는 포즈) | `Asset/Docent/T_Lexi_Full.png` |
| 말풍선 옆 작은 렉시 | `Asset/Docent/T_Lexi_Face.png` |
| 목적지 버튼 `>` | `Asset/DinoCard/chevron_right.png` |
| 스캔 코너 브래킷 | `Asset/Bottom_Bar/T_ScanFrame.png` |
| 셔터 버튼 링 | `Asset/Bottom_Bar/T_Circle.png` |
| 카메라 아이콘 | `Asset/Bottom_Bar/photo_camera.png` |
| 카메라 전환 | `Asset/Docent/refresh.png` |
| 둥근 사각 배경 | `Asset/Docent/T_RoundRect_R32.png` |
| POI 마름모 마커 | `Content/UI/Nav/Markers/T_Marker_*` |
| 공룡 POI 아이콘 | `Content/UI/Nav/Icons/T_Icon_*` |

### GPT 가 아니라 Material Symbols 에서 받을 것

기존 아이콘들(`navigation`, `photo_camera`, `refresh`, `chevron_right`,
`menu_book`, `travel_explore` ...)은 전부 Google Material Symbols 다.
GPT 에 그리게 하면 선 두께와 모서리 처리가 달라 한 화면에 섞였을 때 티가 난다.
같은 출처에서 받는 것이 맞다.

<https://fonts.google.com/icons> 에서 아래를 받는다.
설정은 기존 아이콘과 맞춰 **Outlined / Weight 400 / Grade 0 / Optical size 24**,
PNG 512px, 흰색.

| 아이콘 이름 | 쓰임 |
|---|---|
| `photo_library` | 스캔 화면 갤러리 버튼 |
| `flash_on` | 스캔 화면 플래시 버튼 |
| `explore` | 지도 나침반 |
| `expand_less` / `expand_more` | 지도 층 선택기 |

### GPT 에 요청할 것 — 층 도면 배경 (판단 필요)

**먼저 알아야 할 문제가 있다.** 지금 지도는 서버가 준 nav 그래프(벽·노드
좌표)를 C++ 이 실시간으로 그린다. 목업처럼 아이소메트릭 렌더 이미지를 깔려면
그 이미지가 그래프 좌표계와 픽셀 단위로 맞아야 한다. 안 맞으면 공룡 마커가
벽 위나 허공에 뜬다. GPT 가 그린 그림은 그 정렬을 보장할 수 없다.

두 가지 길이 있다.

- **(a) 벡터 유지, 스타일만 개선** — 배경 이미지를 쓰지 않고 벽 색·두께,
  바닥 채움, 그림자, 조명 느낌을 C++ 에서 조정한다. 정렬 문제가 없고
  층이 늘어나도 그대로 동작한다. 에셋 요청 불필요.
- **(b) 목업 그대로 이미지 배경** — GPT 도면을 깔고 스케일·오프셋을 수동으로
  맞춘다. 맵이나 층이 바뀌면 매번 다시 맞춰야 한다.

**(a) 를 권한다.** 목업의 인상은 대부분 색과 조명에서 오고, 그건 벡터로도
상당히 재현된다. (b) 로 가려면 그때 REQ-003 으로 따로 연다.

### [응답]

(사람이 결정을 적거나, GPT 응답을 붙여넣으세요)

### 검증 결과

(Claude 가 채움)

---

## REQ-001 — 앱 런처 아이콘

**상태: OPEN**
요청자: Claude / 등록: 2026-09-10

### 배경

지금 폰 런처에 언리얼 기본 'U' 로고가 뜬다. 커밋 `6b51c29` 가
"앱 아이콘 설정" 이라고 되어 있지만 실제로는 `ApplicationDisplayName`
(앱 이름) 만 넣었고 이미지 파일은 한 장도 없다. `Build/Android/res/`
폴더 자체가 없어서 엔진 기본 아이콘이 그대로 패키징된다.

### 필요한 것

`docs/ai-handoff/inbox/` 에 아래 두 파일을 **이 이름 그대로** 저장한다.

| 파일명 | 크기 | 배경 | 용도 |
|---|---|---|---|
| `icon_master.png` | 1024x1024 | 불투명(꽉 참) | 레거시 런처 아이콘 |
| `icon_foreground.png` | 1024x1024 | 투명 | 적응형 아이콘 전경 레이어 |

그리고 적응형 아이콘 배경색 16진수 값 하나 (예: `#0B1E3D`).

두 장이 필요한 이유: 안드로이드 8 이상은 적응형 아이콘을 쓴다. 전경/배경
레이어가 없으면 삼성 런처가 흰 원 안에 이미지를 우겨넣어 지저분해진다.
갤럭시 S25 에서 쓸 것이므로 둘 다 필요하다.

해상도별(36~192px) 리사이즈는 Claude 가 스크립트로 처리한다.
GPT 에게 여러 크기를 요구하지 않는다.

### [GPT 프롬프트]

아래 블록을 통째로 ChatGPT 에 붙여넣는다. 레퍼런스 이미지가 있다면
같이 첨부한다.

```
공룡 박물관 AR 앱 "TimeMachineAR" 의 안드로이드 런처 아이콘을 만들어 주세요.

[캐릭터]
앱의 마스코트는 AI 도슨트 '렉시' 입니다. 첨부한 레퍼런스를 따르되,
핵심 특징은 이렇습니다.
- 둥근 흰색 헬멧을 쓴 귀여운 로봇 얼굴
- 검은 바이저 안에 밝은 청록색(cyan) 눈 두 개, 부드럽게 빛남
- 양옆에 흰색 헤드폰
- 머리 위 가느다란 안테나, 끝에 빛나는 구슬
- 전체적으로 파란색 글로우, 매끈한 유광 재질

[스타일]
- 3D 렌더링 느낌의 깔끔한 마스코트 아이콘
- 정면을 보는 얼굴 클로즈업
- 배경은 짙은 남색 계열 그라데이션
- 48픽셀로 줄여도 알아볼 수 있게 단순하고 대비가 강할 것

[반드시 지킬 것]
- 글자, 문자, 숫자를 절대 넣지 말 것
- 정확한 정사각형, 1024 x 1024 픽셀, PNG
- 모서리를 둥글게 깎지 말 것 (런처가 알아서 마스크를 씌움)
- 테두리 액자나 워터마크 없음
- 캔버스 밖으로 나가는 그림자 없음

[결과물 2개]
1) icon_master.png
   - 1024x1024, 불투명 배경
   - 그림이 캔버스를 가장자리까지 꽉 채울 것

2) icon_foreground.png
   - 1024x1024, 배경 완전 투명 (알파 채널)
   - 렉시 캐릭터만, 배경 없이
   - 캐릭터가 중앙 66% 영역(약 676x676) 안에 들어올 것.
     바깥 17% 는 런처가 잘라내는 영역이라 비어 있어야 함

3) 배경 레이어로 쓸 단색 16진수 값 하나를 알려줄 것

[마지막에 아래 형식으로 답할 것]
[응답]
- REQ-001
- 생성: 완료 / 실패
- 파일: icon_master.png, icon_foreground.png
- 배경색: #______
- 참고사항: (지키지 못한 조건이 있으면 여기 적을 것)
```

### [응답]

(ChatGPT 응답을 여기에 붙여넣으세요)

### 검증 결과

(Claude 가 채움)

---
